import React from 'react';
import {SafeAreaView, Text} from 'react-native';
import styles from './styles';
const Notifications = () => {
  return (
    <SafeAreaView>
      <Text>This is the notifications component</Text>
    </SafeAreaView>
  );
};

export default Notifications;
