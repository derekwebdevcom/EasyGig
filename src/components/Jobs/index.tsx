import React from 'react';
import {SafeAreaView, Text} from 'react-native';
import styles from './styles';
const Jobs = () => {
  return (
    <SafeAreaView>
      <Text>This is the Jobs Component</Text>
    </SafeAreaView>
  );
};

export default Jobs;
