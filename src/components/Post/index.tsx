import React from 'react';
import {SafeAreaView, Text} from 'react-native';
import styles from './styles';
const Post = () => {
  return (
    <SafeAreaView>
      <Text>This is the posts component</Text>
    </SafeAreaView>
  );
};

export default Post;
